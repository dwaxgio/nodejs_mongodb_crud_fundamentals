<%- include("template/cabecera", {titleCabecera: "index"}) %>

<div class="container">
  <h1>CRUD Mongo</h1>
  <a class="btn btn-success btn-block my-4" href="/mascotas/crear">Crear nueva mascota</a>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">id</th>
        <th scope="col">nombre</th>
        <th scope="col">descripción</th>
        <th scope="col">editar</th>
      </tr>
    </thead>
    <tbody>
      <% if (arrayMascotas.length > 0) { %> <% arrayMascotas.forEach(mascota =>
      { %>
      <tr>
        <td><%=mascota.id %></td>
        <td><%=mascota.nombre %></td>
        <td><%=mascota.descripcion %></td>
        <td>
          <a href="/mascotas/<%=mascota.id %>" type="button" class="btn btn-warning">Editar</a>
        </td>
      </tr>
      <% }) %> <% } %>
    </tbody>
  </table>
</div>

<%- include("template/footer") %>
